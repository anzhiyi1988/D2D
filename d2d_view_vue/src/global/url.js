const server = "http://172.16.193.19:8090/d2d";

const url = {
    model: {
        GroupTree: `${server}/model/group/tree`,
        LogicGroupList: `${server}/model/logic/group/list`
    }
};

export default url;
